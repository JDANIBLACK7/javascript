'use strict'

function suma(a,b){
    console.log(`La suma entre ${a} + ${b} es igual a ${a+b}`);
}
suma(25,70);

const suma2 = function(c,d){
    console.log(`La suma entre ${c} + ${d} es: ${c+d}`);
}
suma2(50,70);